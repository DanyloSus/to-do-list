//internal imports
import Loading from "@/elements/Form/Loading";

//import from libraries
import { Box } from "@mui/material";

//loading's element
const LoadingEllement = () => {
  return (
    <Box width="100vw" height="100vh">
      <Loading />
    </Box>
  );
};

export default LoadingEllement;
